# Create Agent

“Create agent” means creating a custom AI agent with its own instructions, tools, and behavior for a specific task or workflow.

For this workspace, the available custom agent is `Explore`. It is intended for fast, read-only codebase searching and analysis. Use it to locate files, trace code paths, and understand the project before making changes.
